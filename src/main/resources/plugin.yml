name: ShopPlugin
version: 1.0.0
main: com.example.shop.ShopPlugin
api-version: 1.21
description: GUI shop with categories, money, and OP free items.
authors: [YourName]
softdepend: [Vault]

commands:
  shop:
    description: Open the shop menu.
    usage: /shop
    permission: shop.use

permissions:
  shop.use:
    description: Allows opening the shop.
    default: true
